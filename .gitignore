node_modules/
dist/
build/
out/
cache/
.next/
target/
*.tsbuildinfo
.env
.env.local
.DS_Store
*.sqlite
*.sqlite-journal
packages/flare-contracts/broadcast/
packages/prover/**/target/
coverage/
packages/flare-contracts/lib/
packages/flare-contracts/out/
packages/flare-contracts/cache/

# Rendered video: large binaries, rebuildable with scripts/video/build.sh
scripts/video/out/

# Throwaway devnet identity for the demo recording. Never commit.
scripts/video/.demo-key.env

# Archives: a .env has been zipped into one before. Never commit either.
*.zip

# Deployer key for the Mesa environment. Testnet, but never commit a key.
scripts/.mesa-deployer.env
scripts/.mesa-mina.env
# zkApp keys written by deployWrappedAsset. Anchored with ** because the
# scripts live under packages/, and a mid-pattern slash is root-relative.
**/.deployed-*.env
